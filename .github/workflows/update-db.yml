# DEPRECATED: This workflow is no longer used.
# resources.json is now the source of truth, not README.md.
# See sync-from-supabase.yml for syncing from Supabase.
# See generate-readme.yml for generating README from resources.json.

name: "[DEPRECATED] Update DB from README"

on:
  # Disabled - only manual trigger allowed
  workflow_dispatch:

jobs:
  update-db:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated."
          echo "resources.json is now the source of truth, synced from Supabase."
          echo "README.md is generated from resources.json."
          exit 1
